# ------------------------------
# 1. API Keys (Required)
# ------------------------------
GEMINI_API_KEY=your_gemini_api_key_here
FRED_API_KEY=your_fred_api_key_here

# ------------------------------
# 2. Admin Panel Credentials
# ------------------------------
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# ------------------------------
# 3. Database Configuration (Optional)
# ------------------------------

# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dipsignal
# Leave commented out if using Docker (it is auto-configured by docker-compose).
# Only set this if running manually (without Docker) or using an external DB.

